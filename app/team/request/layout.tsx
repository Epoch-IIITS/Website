import type { ReactNode } from "react";
export const metadata = { title: "Request to join the directory — Epoch Team" };
export default function RequestLayout({ children }: { children: ReactNode }) {
  return children;
}
